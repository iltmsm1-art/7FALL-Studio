7FALL™ STUDIO v3.0 — GITHUB UPLOAD

Upload ALL of these items to the ROOT of the SevenFall-Studio repository:

  index.html          (replace the current landing page)
  game.html           (replace only if this package is newer than your live copy)
  assets/             (upload the entire folder and its four PNG files)

Keep your existing icons, manifest, service worker, license and README files.

After GitHub Pages finishes deploying, press Ctrl+F5 to refresh the live site.

Live flow:
  index.html = marketing landing page
  game.html  = playable game
  assets/    = landing-page screenshots
